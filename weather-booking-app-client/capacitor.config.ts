import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'weather-booking-app-client',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
